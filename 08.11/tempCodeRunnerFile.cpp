        Car(string name, int year, int cost, string colorName, string brandName): Color(colorName), Brand(brandName) {
            modelName = name;
            modelYear = year;
            price = cost;

        }